# 🚀 FlyProx GitHub Pages Deployment Plan

## 📋 Общий план развертывания

### Этап 1: Подготовка к статическому экспорту
1. **Анализ текущего состояния**
   - ✅ Next.js 15.5.4 с React 19.2.0
   - ✅ Pages Router (уже готов для static export)
   - ✅ Tailwind CSS для стилей
   - ✅ localStorage для данных формы

2. **Изменения в next.config.mjs**
   - Добавить `output: 'export'`
   - Добавить `trailingSlash: true` для GitHub Pages
   - Настроить `assetPrefix` для корректных путей

3. **Обновление package.json**
   - Добавить скрипт `"export": "next build && next export"`
   - Убедиться что `"homepage"` поле установлено

### Этап 2: Создание GitHub Actions Workflow
1. **Создание `.github/workflows/deploy.yml`**
   - Автоматическая сборка при push в main
   - Установка зависимостей
   - Запуск export команды
   - Деплой в gh-pages ветку

2. **Настройка permissions**
   - Pages: write
   - id-token: write

### Этап 3: Конфигурация домена flyprox.com
1. **GitHub Pages настройки**
   - Включить GitHub Pages
   - Установить источник: gh-pages ветка
   - Добавить кастомный домен

2. **DNS настройки**
   - A-записи для GitHub Pages
   - CNAME запись в репозитории

### Этап 4: Тестирование страниц
1. **Порядок тестирования:**
   - 1. Главная страница (index.tsx)
   - 2. Страница цен (pricing.tsx)
   - 3. Регистрация (sign-in.tsx)
   - 4. Спасибо (thank-you.tsx)
   - 5. Помощь (help-center.tsx)

## 🔧 Детальные шаги выполнения

### Шаг 1: Конфигурация Next.js для static export

```javascript
// next.config.mjs изменения
const nextConfig = {
  output: 'export',              // Статический экспорт
  trailingSlash: true,           // Для GitHub Pages
  images: {
    unoptimized: true            // Для статического хостинга
  },
  assetPrefix: process.env.NODE_ENV === 'production'
    ? '/flyprox/'
    : '',
  basePath: process.env.NODE_ENV === 'production'
    ? '/flyprox'
    : ''
}
```

### Шаг 2: GitHub Actions Workflow

```yaml
# .github/workflows/deploy.yml
name: Deploy to GitHub Pages
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm ci
      - run: npm run export
      - uses: actions/configure-pages@v4
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./out
      - uses: actions/deploy-pages@v4
```

### Шаг 3: Обновление package.json

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next build && next export",
    "start": "next start"
  },
  "homepage": "https://flyprox.com"
}
```

### Шаг 4: CNAME файл

```text
// CNAME файл в корне
flyprox.com
```

## 🧪 План тестирования

### Тестирование главной страницы
- [ ] Загрузка: https://flyprox.com
- [ ] Стили: Tailwind CSS применяются корректно
- [ ] Навигация: Все кнопки работают
- [ ] CTA: "Get Started" ведет на /sign-in

### Тестирование страницы цен
- [ ] URL: https://flyprox.com/pricing
- [ ] Цены: Корректные минуты (495, 995, и т.д.)
- [ ] Калькулятор: Интерактивный слайдер работает
- [ ] Кнопки: Redirect на sign-in работают

### Тестирование регистрации
- [ ] URL: https://flyprox.com/sign-in
- [ ] Форма: Все поля работают
- [ ] Валидация: Проверка обязательных полей
- [ ] Сохранение: localStorage работает
- [ ] Redirect: На thank-y-ou страницу

### Тестирование благодарности
- [ ] URL: https://flyprox.com/thank-you
- [ ] Дизайн: Минималистичный стиль
- [ ] Сообщение: "Thank you for signing up!"

### Тестирование помощи
- [ ] URL: https://flyprox.com/help-center
- [ ] FAQ: Аккордеон работает
- [ ] Поиск: (удален, проверяем отсутствие)

## 📝 Чек-лист перед деплоем

1. **Код готов**
   - [ ] Все страницы конвертированы в static
   - [ ] Пути к ресурсам абсолютные
   - [ ] localStorage данные работают
   - [ ] Redirect функции корректны

2. **Конфигурация**
   - [ ] next.config.mjs обновлен
   - [ ] package.json скрипты добавлены
   - [ ] GitHub Actions workflow создан
   - [ ] CNAME файл добавлен

3. **Тестирование локальное**
   - [ ] `npm run export` работает
   - [ ] out/ папка создана
   - [ ] Все страницы в out/ существуют
   - [ ] Локальный сервер статических файлов работает

## 🎯 Финальная проверка

После деплоя проверить:
- [ ] Сайт доступен по https://flyprox.com
- [ ] SSL сертификат работает
- [ ] Все страницы загружаются без ошибок
- [ ] Форма регистрации функциональна
- [ ] localStorage сохраняет данные
- [ ] Redirect между страницами работает
- [ ] Мобильная адаптация корректна

## 🔄 Процесс исправлений

Если страница не работает:
1. Проверить консоль браузера на ошибки
2. Проверить сетевые запросы
3. Проверить пути к ресурсам
4. Исправить и повторить деплой
5. Тестировать только измененную страницу
6. После успешного теста переходить к следующей

Этот план обеспечивает пошаговое развертывание с проверкой каждой страницы и гарантирует работающий сайт на flyprox.com.